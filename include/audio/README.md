# Directory: include/audio

Purpose: Audio asset definitions exposed to both engine and UI.

## Files
- `media_clip.h`: Describes `AudioMediaClip` buffers and declares the WAV loader (`audio_media_clip_load_wav`) and lifetime management (`audio_media_clip_free`).
